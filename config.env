# minarch-cores Configuration

# Build options
JOBS=10                     # Parallel build jobs (M4 has 10 performance cores)
                            # Adjust for your system: sysctl -n hw.ncpu

# Cores that have minarch-specific patches (space-separated)
# These will be built separately with patches applied
PATCHED_CORES=gambatte

# Recipe system handles all core selection and build flags
# See: libretro-super/recipes/linux/cores-linux-{arm7neonhf,aarch64}
# - armv7: 134 YES cores with ARM_NEON=1, CORTEX_A7=1, ARM_HARDFLOAT=1
# - aarch64: 137 YES cores (includes mupen64plus_next with dynarec)
